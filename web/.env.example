# Masky OAuth client id (mkc_…) — create one with scripts/register-oauth-client.sh
# Copy this file to .env.local and fill in the value. The client id is public;
# the client SECRET must never appear here (the web app uses PKCE, no secret).
VITE_MASKY_CLIENT_ID=
